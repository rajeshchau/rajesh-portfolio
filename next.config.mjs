/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ext.same-assets.com',
      'github.com',
      'www.instagram.com',
      'linkedin.com',
      'raw.githubusercontent.com',
    ],
  },
};

export default nextConfig;
